module.exports = {
    RECAPTCHA : {
        SITE_KEY : process.env.RECAPTCHA_SITE_KEY,
        SECRET_KEY : process.env.RECAPTCHA_SECRET_KEY,
        options : {
            hl : 'fa'
        }
    }
}